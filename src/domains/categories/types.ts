/**
 * Domínio `categories`.
 *
 * CRUD administrativo implementado na Fase 2 (ver
 * `src/domains/categories/service.ts`).
 */
export type { Category, CategoryStatus } from "@/lib/validation/category.schema";
