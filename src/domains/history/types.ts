/**
 * Domínio `history`.
 *
 * Estrutura documentada em `history/{userId}/items/{historyId}`
 * (ver docs/architecture/firestore-model.md). O recurso completo será
 * implementado em uma fase futura — não implementado nesta fundação técnica.
 */
export type { History, HistoryType } from "@/lib/validation/history.schema";
