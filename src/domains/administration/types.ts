/**
 * Domínio `administration`.
 *
 * Reservado para o painel administrativo (CRUD de conteúdos, categorias,
 * especialistas, versionamento e publicação). Nenhuma funcionalidade
 * administrativa é implementada nesta fase — apenas o papel `administrator`
 * já existe no modelo de perfis para viabilizar a evolução futura.
 */
export {};
